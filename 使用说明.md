# GoBackup 使用说明（Windows + MySQL）

本文件针对本机环境（Windows / PowerShell）说明怎么跑起来、怎么配 MySQL、以及常见坑。
项目本体是 [GoBackup](https://gobackup.github.io)，这里是带 Web 管理界面的分支。

---

## 1. 环境准备

### MySQL 命令行工具

`mysqldump.exe` / `mysql.exe` 必须在 PATH 里，否则备份会失败。

本机已经配好了，无需再操作：

```
C:\Program Files\MySQL\MySQL Server 8.0\bin
```

这个路径**已经在用户环境变量 Path 里**，并且验证可用：

```powershell
mysql --version     # C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe  Ver 8.0.46 for Win64
```

如果你换了机器或清空过 PATH，用这条命令补回去（改完要重开终端）：

```powershell
$bin = "C:\Program Files\MySQL\MySQL Server 8.0\bin"
$old = [Environment]::GetEnvironmentVariable("Path", "User")
if ($old -notlike "*$bin*") {
  [Environment]::SetEnvironmentVariable("Path", "$old;$bin", "User")
}
```

> 备份里还会用到 `sh` 来执行 `before_script` / `after_script`。
> Windows 上要装 Git for Windows，并把 `C:\Program Files\Git\usr\bin` 加进 PATH，脚本功能才可用。
> 不用脚本就不需要。

### 状态目录

GoBackup 会把日志写到状态目录：

- 设置了环境变量 `GOBACKUP_DIR` → 用它
- 否则用 `~/.gobackup`（Windows 上是 `C:\Users\<你>\.gobackup`）

程序启动时会自动创建这个目录，**不需要手动建**。

---

## 2. 配置文件 gobackup.yml

放在项目根目录即为默认查找路径，也可以用 `-c` 显式指定。

当前配置备份 `172.21.17.130` 上的 `venus` 库：

```yaml
web:
  enabled: true
  host: 127.0.0.1
  port: 2703
  username: admin
  password: admin123

models:
  venus:
    description: "venus 数据库备份"
    schedule:
      every: "1day"
      at: "03:30"
    compress_with:
      type: tgz
    storages:
      local:
        type: local
        keep: 14
        path: backups/venus
    databases:
      venus:
        type: mysql
        host: 172.21.17.130
        port: 3306
        database: venus
        username: root
        password: "Xinyu@MysqlAccess2024"
        args: >-
          --default-character-set=utf8mb4
          --single-transaction
          --quick
          --ssl-mode=DISABLED
          --no-tablespaces
          --set-gtid-purged=OFF
          --column-statistics=0
```

### 你给的 JDBC URL 参数怎么映射到 mysqldump

JDBC 参数是 Java 驱动专用的，`mysqldump` 不认识，对应关系：

| JDBC 参数 | mysqldump 等价写法 | 说明 |
|---|---|---|
| `characterEncoding=UTF-8` | `--default-character-set=utf8mb4` | 字符集 |
| `useSSL=false` | `--ssl-mode=DISABLED` | 关闭 SSL |
| `serverTimezone` | — | 纯 Java 驱动行为，不用管 |
| `zeroDateTimeBehavior` | — | 同上 |
| `transformedBitIsBoolean` | — | 同上 |
| `allowMultiQueries` | — | 同上 |

另外三个参数是**这个项目建议加的**，不是 JDBC 的对应物：

- `--single-transaction`：InnoDB 表不锁表、一致性导出（MyISAM 表无效）
- `--quick`：大表不占内存
- `--no-tablespaces`：缺 `PROCESS` 权限时避免报错
- `--set-gtid-purged=OFF`：避免 8.0 上的 GTID 报错
- `--column-statistics=0`：避免 8.0 客户端连 5.7 服务端的兼容报错

### 调度写法

`schedule` 有两种写法，二选一：

```yaml
schedule:
  every: "1day"      # 也支持 30s / 5m / 2h / 1week / 1month
  at: "03:30"        # 每天这个时刻
```

```yaml
schedule:
  cron: "30 3 * * *" # 5 段 cron
```

不写 `schedule` 就只手动触发布备。想临时停掉计划但保留配置：

```yaml
schedule:
  enabled: false
  every: "1day"
  at: "03:30"
```

---

## 3. 怎么运行

先构建（改了前端要按这个顺序，因为前端产物是编译进 exe 的）：

```powershell
cd web; pnpm install; pnpm build; cd ..
go build -o dist\gobackup.exe .
```

> 顺序很重要：`go:embed dist` 在编译时把 `web/dist` 打进 exe。
> 只跑 `pnpm build` 不重新 `go build`，页面不会变。

### 手动备一次

```powershell
.\dist\gobackup.exe perform -m venus -c .\gobackup.yml
```

备份产物落在 `backups\venus\`，文件名形如 `venus_2026.09.18.15.04.05.tar.gz`。

### 常驻 + Web 界面（推荐）

```powershell
.\dist\gobackup.exe run -c .\gobackup.yml
```

然后浏览器打开 <http://127.0.0.1:2703>，账号 `admin`，密码 `admin123`。

`run` 是**前台**运行，占住当前终端；计划任务和 Web 服务都在这个进程里。

### 命令一览

```
commands:
  perform  立即备份一次
  run      前台常驻（跑计划 + Web UI）
  start    后台守护进程运行
  stop     停止正在运行的 run / start 实例
  restart  重启实例（支持后台平滑重启及 -f 前台重启）
  help     帮助
```

---

## 4. 怎么停止服务

### 用内置命令（推荐）

```powershell
.\dist\gobackup.exe stop
```

```
2026/09/18 16:04:07 Sent stop signal to GoBackup (pid 19224).
```

`stop` 靠 pid 文件找到目标进程：

- `run` / `start` 启动时会把自身 pid 写到 `$GOBACKUP_DIR/gobackup.pid`
  （默认 `~/.gobackup/gobackup.pid`，Windows 上是 `C:\Users\<你>\.gobackup\gobackup.pid`）
- 进程退出时会删掉这个文件，`stop` 也会在信号发出后清理它
- 没有实例在跑时，`stop` 会明确报错并以退出码 1 结束：
  `no running GoBackup instance (no pid file at ...)`
- pid 文件指向的进程已经没了，`stop` 会提示并顺手删掉过期的 pid 文件

停止是否“优雅”分平台：

| 平台 | `stop` 的做法 | 效果 |
|---|---|---|
| Linux / macOS | 发 `SIGTERM` | 进程收到信号后停掉调度器、清理 pid 文件再退出 |
| Windows | 直接终止进程 | 立刻退出，pid 文件由 `stop` 自己清理 |

> 两种方式都会打断正在跑的备份，可能留下半截 dump 文件。
> 想等这一轮跑完，先看日志或 Web 的「运行日志」页，确认没有备份在跑再停。

### 如果 stop 不可用

按你的部署方式选：

| 部署方式 | 停止命令 |
|---|---|
| 前台 `run` | `Ctrl+C`（代码里注册了 SIGINT 处理，会走优雅退出） |
| `nohup ... run &` | `gobackup stop`，或 `pkill -f gobackup-linux-amd64` |
| `start` 起的守护进程 | `gobackup stop`，或 `kill "$(cat ~/.gobackup/gobackup.pid)"` |
| systemd 托管 | `sudo systemctl stop gobackup` |
| Docker | `docker stop <容器名>` |

排查时常用：

```bash
pgrep -af gobackup              # 找到进程和启动参数
ps -o pid,etime,cmd -p <pid>    # 看已经跑了多久
cat ~/.gobackup/gobackup.pid    # gobackup stop 会读这个文件
ss -ltnp | grep 2703            # 确认 Web 端口是谁占的
```

### 关于 `-s stop` / `kill -HUP`

这两个都不要用：

- `-s stop` / `-s quit` / `-s reload` 是 go-daemon 留下的参数，
  本项目从未调用 `daemon.ServeSignals()`，它们不会被解析，
  直接跑会报 `Incorrect Usage: flag provided but not defined: -s`。
  现在 `stop` 命令已经取代了它。
- README 里写的 `kill -HUP` 热重载同样没有接线。用 HUP/QUIT 会按系统默认行为终止进程。
---

## 5. 怎么重启服务

使用内置的 `restart` 命令（推荐，跨平台支持 Windows / Linux / macOS）：

```powershell
# Windows
.\dist\gobackup.exe restart -c .\gobackup.yml

# Linux
./gobackup-linux-amd64 restart -c ./gobackup.yml
```

- 默认行为：平滑停止已有正在运行的实例，等待端口与资源释放后，在后台启动新守护进程并记录运行日志。
- 如需前台常驻运行（例如排查故障调试），添加 `-f` 或 `--foreground` 参数：
  ```powershell
  .\dist\gobackup.exe restart -f -c .\gobackup.yml
  ```

---

## 5. Web 界面

- **配置管理** `/settings/config`：可视化编辑这个 YAML。
  改完点「保存并重载」→ 校验 → 原子替换文件 → 运行时热加载，不用重启进程。
- **任务中心** `/tasks`：所有备份任务的实时进度与任务日志。
  进行中的任务显示阶段（准备 / 导出数据库 / 打包 / 压缩 / 加密 / 分卷 / 上传归档 / 清理）、
  整体百分比、已处理字节数和实时滚动的日志。
- **运行日志** `/logs`：
  - 「实时日志」跟随服务输出，默认回看末尾 200 行，支持等级筛选与高亮
    （错误红色、警告黄色、调试灰色，`[GIN] ... | 5xx |` 也算错误）、关键词过滤、折行、搜索、自动跟随。
  - 「备份记录」保留最近 100 次备份的结果、耗时、归档文件与进度。
- **备份概览** `/overview`：看模型、立即触发备份、浏览远端归档。

### 下载归档

「备份概览 → 浏览远端归档」里的文件名和「下载」按钮都能把归档存到本地：

- **local 存储**：没有可跳转的 URL，后端直接把文件流式发给浏览器
  （带 `Content-Disposition`，浏览器按原文件名保存），不再返回
  `Local is not support download`。
- **S3 / WebDAV 等远端存储**：仍然 302 跳到预签名地址，由浏览器直接向对象存储下载。
- 归档动辄几百 MB，所以**不会**先用 `fetch` 把整个响应读进内存再存盘，
  而是走浏览器原生下载：点「下载」时先调 `GET /api/download/ticket` 换一张
  **一次性票据**（2 分钟有效、只对本次这个文件生效），再让浏览器带着
  `?ticket=...` 去下载。这样既不把账号密码写进 URL，也能正确跟随 302 跳转。
  票据用过即失效，换个文件或换到别的接口都不认。
- 下载路径会被限制在存储根目录内：`path` 里带 `..`、文件不存在、或指向目录
  （分卷归档）分别返回 404/404/404，不会读到目录外的文件。

> 服务日志里的 `Other users are able to access gobackup.yml with mode -rwxr-xr-x`
> 是提醒配置文件权限过宽。配置里有数据库口令和 Web 口令，建议
> `chmod 600 gobackup.yml`（配置编辑器保存时会沿用当前文件权限，不会被改回去）。

配置页的几个设计点：

- 未配置的可选功能（加密 / 分片 / 归档等）只显示一行「未配置 + 启用」，
  不会再铺一堆空输入框。点「启用」才展开。
- 执行间隔、执行时刻、Cron、分片大小等字段是**可下拉可选、也可手填**的输入框。
- 「是否启用」显示的是**运行时真正生效的值**（计划任务默认是启用的）。
- 保存前的确认弹窗用中文描述你的改动，比如
  `设置 模型 venus › 计划 › 执行时刻 = 03:30`，原始 JSON 折叠在下面。

### 前端缓存与「打不开页面」

- `/assets/*`（文件名带内容 hash）响应 `Cache-Control: public, max-age=31536000, immutable`，
  第二次打开页面几乎不再下载资源。整包约 2 MB，以前每次刷新都要重下。
- `index.html` 和前端路由（`/overview`、`/tasks`…）是 `Cache-Control: no-cache` + `ETag`，
  每次回源校验：换了二进制之后刷新一次就能拿到新版本，不会出现「服务端已经是新版、
  浏览器还拿着旧 index.html 去要已经改名的 chunk」。
- 缺失的 `/assets/*.js` 返回 **404**。以前它会被 SPA 回退成 `200 text/html`，浏览器把 HTML
  当模块脚本执行，只留下一条 MIME 报错和一片白屏，从界面上完全看不出原因。
- 所以遇到白屏先 `Ctrl+Shift+R` 强刷一次；还不行就看 F12 → Network 里哪个资源不是 200。

### 选数据库和表

数据库条目里的「数据库名 / 指定的表 / 排除的表」是联动的，三个字段共用一次探测结果
（以前每个字段各连一次库，点两次按钮就要连两次）：

- **数据库名**：可以直接手填，也可以点「获取列表」拉取实例上的数据库后下拉选择。
- **指定的表 / 排除的表**：
  - 顶部「目标数据库」下拉展开时才会去读库列表，避免每次打开页面就连库；
  - 库不多时（≤12 个）会把其它库列成**可一键切换的标签**，切换后自动按新库重新拉表；
  - 表用**双栏穿梭框**：左侧带搜索框、全选、取消全选，右侧显示已选并可单个移除或一键清空，
    底部显示「共 N 张表 · 已选 M」；
  - 数据库里没有列出来的表名（视图、权限受限的表）可以用底部输入框手动添加，回车即可；
  - 已配置但不在当前库列表里的表不会被丢掉，仍然显示在右侧。
- **「备份所有数据库」与按表筛选互斥**：开启后表选择器会替换成说明和一个
  「关闭「备份所有数据库」并按表选择」按钮。这样就不会再出现「配置能保存、备份时才报错」的情况
  （MySQL 明确不允许 `all_databases` 和 `tables/exclude_tables` 同时使用）。
- 探测失败（连不上、缺客户端、Redis/etcd 这类没有表概念的库）会在字段下方显示后端返回的中文原因，
  不会弹一堆报错提示。

---

## 5.1 备份进度与并发规则

### 进度是怎么算的

一次备份被拆成若干阶段，每个阶段有固定权重，整体进度 = 已完成阶段权重 +
当前阶段完成度 × 当前阶段权重：

| 阶段 | 权重 | 进度来源 |
|---|---|---|
| 准备 | 2% | 瞬时 |
| 导出数据库 | 45% | 导出目录的实时字节数（总量未知，进度条按时间缓慢推进） |
| 打包 | 4% | 瞬时 |
| 压缩 | 16% | 归档文件的实时字节数 |
| 加密 | 2% | 瞬时 |
| 分卷 | 1% | 瞬时 |
| 上传归档 | 29% | 上传字节数 / 归档总大小（精确百分比） |
| 清理 | 1% | 瞬时 |

没有配置的阶段会被跳过，并按实际参与阶段的总权重归一化，所以进度条一定能走到 100%。
进度实时保存在内存里，任务中心每秒刷新一次；同时按秒节流写进运行记录 JSON，
刷新页面或重启后仍能看到最后一次状态。

### 并发规则

- **同一个数据库环境同一时间只允许一个任务**。判定依据是数据库配置里的
  连接目标（`type` + `socket`/`uri`/`endpoints`/`host:port` + `database`/`path`/`bucket`）。
  冲突时网页会直接提示「同一数据库环境已有进行中的任务……」并返回 409，
  计划任务则会跳过本次并在日志里记一条 warning。
- **同一个模型不允许并发**（多个任务会共用同一个临时目录）。
- 不同数据库、不同模型之间可以并行执行，互不影响。

如果两个数据库配置指向同一台机器的不同库、但你希望它们互斥（或反过来），
可以用 `exclusive_key` 手工指定互斥组：**拥有相同 `exclusive_key` 的数据库不会并发执行**。

```yaml
databases:
  venus:
    type: mysql
    host: 172.21.17.130
    database: venus
    exclusive_key: venus-cluster   # 同组的其它库也会被互斥
```

---

## 6. 常见问题

### 日志页空白 / Web 打不开，报 `open .gobackup\gobackup.log: The system cannot find the path specified`

老版本的坑：Windows 没有 `HOME` 环境变量，状态目录退化成相对路径 `.gobackup`，
日志文件建不出来，HTTP 服务直接不启动。

现在的代码会自动创建状态目录（`os.UserHomeDir()` 回退到 `USERPROFILE` + `MkdirAll`），
这个问题已经修好。

### 「实时日志」页没有任何输出

老版本的 `/api/log` 复用启动时打开的单个文件句柄 + `fls.SeekLine(-50, SeekEnd)`：

- 日志不足 50 行时 `SeekLine` 返回 `EOF` 并把偏移量丢回文件开头，
  于是整个日志文件被重发，文件大时浏览器直接卡死（表现为“没有渲染”）；
- 多个页面同时看日志会互相抢同一个文件句柄，导致丢行、重复行；
- 客户端断开后读取 goroutine 会永久阻塞，泄漏 goroutine。

现在 `/api/log` 每个连接自己打开文件、从末尾 N 行开始跟随，支持 `?tail=` / `?offset=`，
轮转与截断都能自动识别，断开即退出。前端也做了空行过滤（用作心跳）、
自动重连（1s→2s→5s 退避）和 5000 行缓冲上限。

### 计划任务不生效，日志里有 `Failed to register job func`

`every` 的值是 `1day` 这类扩展单位时，底层 gocron 只认 Go 标准时长单位（`h` / `m` / `s`），
注册会直接失败。

现已修复：`1day` / `2weeks` 会先归一化成标准时长，`at` 时刻改用「计算下一次该时刻」的方式实现
（gocron 对 duration 单位禁用 `At()`）。`every: "1day"` + `at: "03:30"` 现在是有效的。

### local 存储的路径

`storage: local` 的 `path` 现在用 `filepath.IsAbs` 判断，`C:\data\backup` 这类
Windows 绝对路径可以正常使用；相对路径仍然相对 `WorkDir`（运行目录）。

上传也改成了 Go 原生复制（原来调用 `cp -a`，而 Windows 上没有 `cp`，
本地存储因此完全不可用）。分卷产生的目录会递归复制，并且复制过程会实时上报字节进度。

### 备份表里的 MyISAM 表

`--single-transaction` 只保证 InnoDB 表的一致性。MyISAM 表多的话需要额外处理。

### 恢复

`mysqldump` 的产物不含 `CREATE DATABASE`：

```bash
mysql -h 172.21.17.130 -u root -p venus < venus_2026.09.18.15.04.05.sql
```

---

## 7. 当前已知问题：数据库连不上

从本机连 `172.21.17.130:3306` 的实测结果：

```
TCP 3306        →  通（能建立连接）
MySQL 握手      →  失败
  ERROR 2013 (HY000): Lost connection to MySQL server at
  'reading initial communication packet', system error: 0
```

`--ssl-mode=DISABLED` / `REQUIRED` / 默认三种模式都是同样的错。
端口通但收不到 MySQL 的握手包，常见原因：

1. **服务端 host_cache 把这个客户端 IP 拉黑了**（失败次数超过 `max_connect_errors`）。
   在一台能连上的机器上执行 `mysqladmin flush-hosts` 或 `FLUSH HOSTS;`。
2. **3306 不是 MySQL 本身** —— 端口转发 / 防火墙 / 负载均衡接受了 TCP，但没有转发数据。
3. 访问策略（安全组、白名单）只放行了 TCP，没放行后续数据包。

**在解决之前，`perform` 和计划备份都会失败**，因为 `mysqldump` 会撞上同一个错误。
建议先在同一台机器上用 `mysql` 客户端验证能连上，再跑备份。

---

## 8. 部署到 Linux 服务器

Windows 上构建好后，`dist/` 里有两个产物：

| 文件 | 说明 |
|---|---|
| `gobackup-linux-amd64` | 裸二进制，31 MB，静态链接（CGO_ENABLED=0） |
| `gobackup-linux-amd64.tar.gz` | 部署包，10.8 MB，解压出 `gobackup`（权限已带 0755） |

构建命令（等同于 `make build_linux_amd64`）：

```powershell
cd web; pnpm build; cd ..
$env:GOOS="linux"; $env:GOARCH="amd64"; $env:CGO_ENABLED="0"
go build -trimpath -ldflags="-s -w" -o dist\gobackup-linux-amd64 .
```

> 打 tar.gz 时不要直接用 Windows 自带的 `tar`：它写出来的成员权限是 0666（没有可执行位）。
> 用 Python 的 `tarfile` 显式写 0755 更省事：
>
> ```powershell
> python -c "import tarfile;t=tarfile.open('dist/gobackup-linux-amd64.tar.gz','w:gz');i=t.gettarinfo('dist/gobackup-linux-amd64','gobackup');i.mode=0o755;t.addfile(i,open('dist/gobackup-linux-amd64','rb'));t.close()"
> ```

打包方式对齐了官方 `install` 脚本（`goreleaser.yml` 的约定：`CGO_ENABLED=0`、
`-s -w`、二进制名 `gobackup`、归档名 `gobackup-<os>-<arch>.tar.gz`），
所以归档里的 `gobackup` 可以直接喂给官方安装脚本。

### 部署包内容

```
gobackup            真正的可执行文件（已重命名，权限 0755）
```

配置和 systemd 单元需要自己放到服务器上（见下面的步骤）。

### 手动部署

> 归档里的 `gobackup` 已经带可执行位，解压后可以直接跑。
> 如果用别的方式拷过去丢了权限，记得 `chmod +x gobackup`。

```bash
# 1. 传上去
scp dist/gobackup-linux-amd64.tar.gz user@server:/tmp/

# 2. 解压安装
ssh user@server
cd /tmp && tar xzf gobackup-linux-amd64.tar.gz
sudo install -m 0755 gobackup /usr/local/bin/gobackup

# 3. 放配置
sudo mkdir -p /etc/gobackup
sudo install -m 0600 gobackup.yml /etc/gobackup/gobackup.yml

# 4. 验证
gobackup --version      # master
gobackup --help         # 能看到 stop 命令
```

### 用 systemd 托管（推荐）

```bash
# 建服务账号和目录
sudo useradd --system --home /var/lib/gobackup --shell /usr/sbin/nologin gobackup
sudo mkdir -p /var/lib/gobackup
sudo chown gobackup:gobackup /var/lib/gobackup /etc/gobackup/gobackup.yml

# 装单元
sudo install -m 0644 gobackup.service /etc/systemd/system/gobackup.service
sudo systemctl daemon-reload
sudo systemctl enable --now gobackup

# 日常操作
systemctl status gobackup
systemctl stop gobackup        # 或者 gobackup stop
systemctl restart gobackup
journalctl -u gobackup -f
```

单元里几个选择的原因：

- `ExecStart` 用 `run` 而不是 `start`。`run` 是前台进程，正是 systemd 要的形态；
  `start` 会 fork 成守护进程，systemd 反而管不住。
- **故意不写 `ExecStop`**。代码里已经注册了自己的 SIGTERM 处理，
  systemd 默认的停止动作就会停掉调度器、清理 pid 文件再退出。
  所以 `systemctl stop gobackup` 和 `gobackup stop` 是等价的。
- `GOBACKUP_DIR=/var/lib/gobackup` + `StateDirectory=gobackup`：
  日志和 pid 文件都落在 systemd 管理、且权限正确的目录里。
- `UMask=0077`：备份文件和配置里有数据库密码。
- 如果你的场景需要 root（例如要读本机某个受限路径），把 `User` / `Group` 两行删掉即可。

### 部署后注意

- 配置里 `storages.local.path` 用的是相对路径 `backups/venus`，
  产物会落在 `WorkingDirectory`（即 `/var/lib/gobackup/backups/venus`）。
  Linux 上也可以用绝对路径，比如 `/data/backups/venus`。
- 服务器要装 `mysqldump`：`sudo apt install mysql-client` 或 `dnf install mysql`。
  用 `before_script` / `after_script` 的话还要 `sh`。
- 跨机器部署时记得确认服务器能不能连上数据库——本机连不上不代表服务器连不上，
  反过来也一样。
- 升级二进制时**先停进程、再删旧文件、最后把新文件移进去**：
  Linux 不允许覆盖正在执行的程序（`cp -f` 会报 `Text file busy`）。
- 升级脚本可以参考 `verify-download.sh` 的顺序：停服务 → 备份旧二进制 → 解包安装 →
  启动 → 自检（`/status` 200、未鉴权 401、带鉴权下载并比对 sha256、一次性票据生效且只能
  用一次、伪造票据 401、路径穿越 404）→ 任何一项失败就自动回滚到升级前的二进制。
